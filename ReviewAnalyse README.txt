The Analyze_review file tells the reason by which the model is predicting any sentiment

I have used topic modelling here.
The LDA model was used for this purpose.
The LDA model is trained on the training dataset that was present
It give the keypoints because of which the model understands to which class does a particular text belongs to

